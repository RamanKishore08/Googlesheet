import React from "react";

const Header = () => {
  return (
    <nav className="navbar navbar-dark bg-primary text-center mb-4"> {/* Added `mb-4` for spacing */}
      <span className="navbar-brand mx-auto">Google Sheet Mimics</span>
    </nav>
  );
};

export default Header;
